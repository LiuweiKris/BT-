## 架构概览
涉及的关键 Java 组件位于 `vendor/qcom/opensource/commonsys/packages/apps/Bluetooth/src/com/android/bluetooth/`:

| 组件 | 路径 | 职责 |
| --- | --- | --- |
| A2dpService | `a2dp/A2dpService.java` | A2DP 音乐播放管理 |
| A2dpStateMachine | `a2dp/A2dpStateMachine.java` | 每设备 A2DP 状态机 |
| HeadsetService | `hfp/HeadsetService.java` | HFP 通话管理 |
| HeadsetStateMachine | `hfp/HeadsetStateMachine.java` | 每设备 HFP 状态机 |
| HeadsetA2dpSync | `hfp/HeadsetA2dpSync.java` | HFP 与 A2DP 的同步桥梁 |
| ActiveDeviceManager | `btservice/ActiveDeviceManager.java` | 管理各 Profile 的活动设备 |
| HeadsetSystemInterface | `hfp/HeadsetSystemInterface.java` | HFP 系统接口（获取通话状态） |


---

## 完整流程分析
### 阶段 1：蓝牙耳机播放音乐
**初始状态**：设备通过 A2DP 连接且正在播放音乐。

1. **A2dpStateMachine** 处于 `Connected` 状态，`mIsPlaying = true`
2. A2DP 音频数据通过 Bluetooth HAL 传输到耳机
3. **HeadsetA2dpSync** 中跟踪 A2DP 播放状态：`mA2dpConnState.put(device, A2DP_PLAYING)`
    - 该状态由 `updateA2DPPlayingState()` (HeadsetA2dpSync.java:416) 监听 `BluetoothA2dp.ACTION_PLAYING_STATE_CHANGED` 广播来维护

---

### 阶段 2：来电（电话铃响）
**触发**：Telecom 框架通知 `phoneStateChanged()` 来电。

#### 2.1 状态更新入口
`HeadsetService.phoneStateChanged()` (HeadsetService.java:2759) 被调用，参数为 `callState = CALL_STATE_INCOMING`。

#### 2.2 暂停 A2DP 音乐
`phoneStateChanged()` 向所有已连接/正在连接的设备发送 `CALL_STATE_CHANGED` 消息 (line 2841)。

`HeadsetStateMachine.processCallState()` (HeadsetStateMachine.java:2458) 处理该消息，调用 `processA2dpState()` (line 2590)。

`processA2dpState()` (HeadsetStateMachine.java:2598) 执行以下逻辑：

```plain
isInCall() || isRinging() == true  →  需要暂停 A2DP
    ↓
HeadsetA2dpSync.suspendA2DP() 被调用
    ↓
如果 A2DP 正在播放 (A2DP_PLAYING)：
  - 设置 mA2dpSuspendTriggered = A2DP_SUSPENDED_BY_CS_CALL
  - 调用 AudioManager.setA2dpSuspended(true)
  - 返回 true（调用者需要等待暂停确认）
  - 将当前 callState 缓存到 mPendingCallStates (line 2609)
    ↓
如果 A2DP 仅连接未播放 (A2DP_CONNECTED)：
  - 设置 mA2dpSuspendTriggered
  - 调用 AudioManager.setA2dpSuspended(true) 阻止新的 A2DP 启动
  - 返回 false（不需要等待）
```

**关键代码**见 HeadsetA2dpSync.java:201-325 (`suspendA2DP` 方法)。

#### 2.3 A2DP 音频流停止
AudioManager 将 `A2dpSuspended=true` 传递给 Audio HAL，A2DP 音频流停止。

`A2dpStateMachine.processAudioStateEvent()` (A2dpStateMachine.java:610) 收到 `AUDIO_STATE_STOPPED` / `AUDIO_STATE_REMOTE_SUSPEND`，设置 `mIsPlaying = false`，广播 `STATE_NOT_PLAYING`。

#### 2.4 暂停确认后的后续处理
HeadsetA2dpSync 监听到 `A2DP_STATE_NOT_PLAYING` 广播（通过 `updateA2DPPlayingState()` line 458-473），确认 A2DP 已暂停后：

+ 调用 `mHeadsetService.sendA2dpStateChangeUpdate()` (line 470)
+ HeadsetService 分发 `A2DP_STATE_CHANGED` 到各 StateMachine

`HeadsetStateMachine.processIntentA2dpPlayStateChanged()` (HeadsetStateMachine.java:2650) 处理：

```plain
isInCall() == true → 发送缓存的来电状态到远端耳机
  遍历 mPendingCallStates，调用 mNativeInterface.phoneStateChange()
  耳机收到来电指示（铃声响起）
```

---

### 阶段 3：接听电话（SCO 音频通道建立）
**触发**：用户接听电话，`phoneStateChanged()` 报告 `numActive=1, callState=CALL_STATE_IDLE`。

#### 3.1 建立 SCO 连接
`processCallState()` (HeadsetStateMachine.java:2458) 检测到 active call：

+ 如果设备是 active device，发送 `CONNECT_AUDIO` 消息建立 SCO
+ 状态机从 `Connected` → `AudioConnecting` → `AudioOn`

**关键代码**见 HeadsetStateMachine.java:1616-1648。

#### 3.2 SCO 音频通道建立后
状态进入 `AudioOn`，通话音频通过 SCO (Synchronous Connection Oriented) 通道传输到耳机。

此时 A2DP 仍处于暂停状态（`A2dpSuspended=true`），无音乐播放。

---

### 阶段 4：挂断电话
**触发**：`phoneStateChanged()` 报告 `numActive=0, numHeld=0, callState=CALL_STATE_IDLE`。

#### 4.1 断开 SCO
`processA2dpState()` 检测到 `!isInCall() && !isRinging()`:

+ 如果 `mPendingCallStates` 非空，先发送 call idle 状态到远端
+ 发送 `DISCONNECT_AUDIO` 断开 SCO

`HeadsetStateMachine.AudioOn` 状态下的 `processAudioEvent(AUDIO_STATE_DISCONNECTED)` (line 1732-1746)：

```plain
!isInCall() && !isRinging() && mPendingCallStates.isEmpty()
    ↓
调用 mHeadsetService.getHfpA2DPSyncInterface().releaseA2DP(mDevice)
```

#### 4.2 恢复 A2DP 音乐
`HeadsetA2dpSync.releaseA2DP()` (HeadsetA2dpSync.java:345) 执行：

```plain
1. 检查是否还有其他 Call/Ring/SCO active → 有则退出不做恢复
2. 重置 mA2dpSuspendTriggered = A2DP_SUSPENDED_NOT_TRIGGERED
3. 调用 AudioManager.setA2dpSuspended(false)    ← 解除 A2DP 暂停
4. 获取 A2dpService 实例
5. a2dpService.setActiveDevice(null)             ← 先清除 Active Device
6. Thread.sleep(100)                             ← 等待 native 层处理
7. a2dpService.setActiveDevice(device)           ← 重新激活设备
```

**关键代码**见 HeadsetA2dpSync.java:345-403。

#### 4.3 播放恢复
AudioManager 通知 Audio HAL `A2dpSuspended=false`，A2DP 音频流恢复。

`A2dpStateMachine.processAudioStateEvent()` 收到 `AUDIO_STATE_STARTED`，设置 `mIsPlaying = true`，广播 `STATE_PLAYING`。

音乐通过 A2DP 恢复播放。

---

## 关键状态流转图
```plain
[Music Playing over A2DP]
        │
        │ phoneStateChanged(INCOMING)
        ▼
[A2DP Suspended] ──→ send RING indicator to headset
        │                    │
        │ user answers call  │
        ▼                    ▼
[SCO Connected]  ←──  phoneStateChanged(ACTIVE)
        │
        │ phoneStateChanged(IDLE, numActive=0)
        ▼
[SCO Disconnected] ──→ releaseA2DP()
        │
        │ AudioManager.setA2dpSuspended(false)
        ▼
[A2DP Resumed - Music Playing]
```

---

## 涉及的关键回调与广播
| 广播/Callback | 发送者 | 接收者 | 作用 |
| --- | --- | --- | --- |
| `ACTION_PLAYING_STATE_CHANGED` | A2dpStateMachine | HeadsetService.mHeadsetReceiver | 通知 HFP A2DP 播放状态变化 |
| `ACTION_CONNECTION_STATE_CHANGED` | A2dpStateMachine | HeadsetService.mHeadsetReceiver | 通知 HFP A2DP 连接状态变化 |
| `CALL_STATE_CHANGED` (消息) | HeadsetService | HeadsetStateMachine | 通知状态机电话状态变化 |
| `RESUME_A2DP` (消息) | HeadsetService/StateMachine 内部 | HeadsetStateMachine | 触发 A2DP 恢复 |
| `A2DP_STATE_CHANGED` (消息) | HeadsetService | HeadsetStateMachine | 通知 A2DP 暂停/恢复完成 |


---

## Qualcomm 特有扩展
相比 AOSP 蓝牙栈，Qualcomm 的代码增加了以下扩展：

1. **TWS+ 支持** (`mIsTwsPlusEnabled`)：支持同时连接左右耳机的 TWS+ 模式，HeadsetA2dpSync 和 HeadsetService 中有 TWS+ SHO (Simultaneous HandOff) 逻辑
2. **LE Audio 共存** (`ApmConstIntf.getQtiLeAudioEnabled()`)：代码中有大量分支处理 LE Audio (BAP) 与传统 A2DP/HFP 的共存
3. **SWB (Super Wideband Speech)** 支持 (`mIsSwbSupportedByRemote`)：在通话建立时动态切换 SWB 编解码器
4. **Split A2DP** (`isSplitA2dpEnabled`)：音频编解码器协商在 Audio HAL 侧完成
5. **ActiveDeviceManagerServiceIntf**：QTI 自定义的活动设备管理接口

---

## 总结
核心交互由 **HeadsetA2dpSync** 桥接模块协调：

+ 来电时通过 `AudioManager.setA2dpSuspended(true)` 暂停音乐
+ 先暂停 A2DP，再发 Ring 指示到耳机（避免部分耳机在 A2DP 活跃时处理异常）
+ 挂断后通过 `releaseA2DP()` → `AudioManager.setA2dpSuspended(false)` → `setActiveDevice()` 恢复音乐
+ 整个流程由 `phoneStateChanged()` 驱动，通过 StateMachine 消息机制异步处理

